import 'package:flutter/material.dart';

class Reposted extends StatelessWidget {
  const Reposted({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const [
        Text('data'),
      ],
    );
  }
}
