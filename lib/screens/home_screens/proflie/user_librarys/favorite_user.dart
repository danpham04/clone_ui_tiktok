import 'package:flutter/material.dart';

class FavoriteUser extends StatelessWidget {
  const FavoriteUser({super.key});

  @override
  Widget build(BuildContext context) {
   return const Center(child: Text('xin chao4'));
  }
}