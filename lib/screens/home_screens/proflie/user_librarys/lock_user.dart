import 'package:flutter/material.dart';

class LockUser extends StatelessWidget {
  const LockUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('xin chao2'));
  }
}
