import 'package:flutter/material.dart';

class SaveUser extends StatelessWidget {
  const SaveUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('xin chao3'));
  }
}
